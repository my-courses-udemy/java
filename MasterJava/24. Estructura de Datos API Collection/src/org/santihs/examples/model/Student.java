package org.santihs.examples.model;import java.util.Objects;public class Student implements Comparable<Student> {    private String name;    private Integer nota;    public Student(String name, Integer nota) {        this.name = name;        this.nota = nota;    }    public String getName() {        return name;    }    public void setName(String name) {        this.name = name;    }    public Integer getNota() {        return nota;    }    public void setNota(Integer nota) {        this.nota = nota;    }    @Override    public String toString() {        return "Alumno{" +                "nombre='" + name + '\'' +                ", nota=" + nota +                '}';    }    @Override    public int compareTo(Student student) {        /*if (this.name == null)            return  0;        return this.name.compareTo(student.getName());*/        /*if (this.nota == student.getNota()) return 0;        else if (this.nota > student.getNota()) return 1;        else return -1; */        return this.name.compareTo(student.getName());        // return Integer.compare(this.nota, student.getNota());    }    @Override    public boolean equals(Object o) {        if (this == o) return true;        if (o == null || getClass() != o.getClass()) return false;        Student student = (Student) o;        return Objects.equals(name, student.name) && Objects.equals(nota, student.nota);    }    @Override    public int hashCode() {        return Objects.hash(name, nota);    }}